apiVersion: apps/v1
kind: Deployment
metadata:
  name: zercle-rust-template
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: zercle-rust-template
  template:
    metadata:
      labels:
        app.kubernetes.io/name: zercle-rust-template
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: server
          image: zercle-rust-template:latest
          ports:
            - containerPort: 8080
              name: http
            - containerPort: 50051
              name: grpc
          envFrom:
            - configMapRef:
                name: zercle-rust-template-config
            - secretRef:
                name: zercle-rust-template-secret
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsUser: 65532
            capabilities:
              drop: ["ALL"]
