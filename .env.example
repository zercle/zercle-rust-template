# Timezone and Locale
TZ=Asia/Bangkok
LANG=C.UTF-8

# Server Configuration
SERVER_PORT=3000
SERVER_HOST=0.0.0.0
SERVER_ENV=local

# Database Configuration
# Uses PostgreSQL 18 as the primary database
DB_DRIVER=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=postgres
DB_SSL_MODE=disable

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-for-development-only-123
JWT_EXPIRATION=3600

# Logging
LOG_LEVEL=info
LOG_FORMAT=json

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# Rate Limiting
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60
